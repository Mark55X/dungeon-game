apply plugin: "java";

sourceSets {
	main {
		java {
			srcDir "src"
		}

		resources {
			srcDir "res"
		}
	}
	
	test {
		java {
			srcDir "test"
		}
	}
}

jar {
	manifest {
		attributes "Main-Class": "com.onn.dungeongame.Launcher"
	}
}

task run(type: Exec, dependsOn: jar, group: 'Run', description: 'Run built jar files') {
	executable 'java'
	args '-jar', "$jar.archivePath"
}
